{
  "name": "@personal-character-agent/agent-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./security": "./src/security/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@personal-character-agent/avatar-core": "workspace:*",
    "@personal-character-agent/memory-core": "workspace:*",
    "@personal-character-agent/shared": "workspace:*"
  }
}
